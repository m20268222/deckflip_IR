// Optional deployment configuration. Only public anon/publishable keys belong here.
// The local server supplies its own same-origin configuration at this URL.
window.IR_POLL_CONFIG = window.IR_POLL_CONFIG || null;
